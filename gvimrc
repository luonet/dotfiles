" disable blinking for all modes (the "a" specifies all modes, and the 0 value
" for blinkon disables blinking):
set guicursor+=a:blinkon0
set guifont=Monaco:h14
" If you want the window as big as possible, you can set these options to very
" large values: https://github.com/b4winckler/macvim/wiki/FAQ
set columns=200
set lines=100
